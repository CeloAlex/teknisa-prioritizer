DATABASE_URL="postgresql://postgres:postgres@localhost:5432/hcm_prioritizer?schema=public"
PORT=3000
JWT_SECRET="change-me-to-a-long-random-string"
# Chave usada pela API de integração (header X-API-Key) para importação de issues e
# clientes por sistemas externos. Deixe vazia para desabilitar essa via de acesso.
INTEGRATION_API_KEY="change-me-to-a-long-random-string"
